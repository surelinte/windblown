using UnityEngine;

/*public class SceneMusicTheme : MonoBehaviour
{
    [SerializeField] private AudioClip theme;
    [SerializeField] private bool restartIfSame = false;

    private void Start()
    {
        // Use your persistent music system
        MusicPlayer.Instance?.PlayMusic(theme, restartIfSame);
    }
}*/
